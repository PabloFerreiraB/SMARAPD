(function () {
    'use strict';

    const app = angular.module('app');

    app.factory('loginService', function ($http, APIConfig, responseService) {
        var defaultRoute = APIConfig.smarAPD_COMPONENTES + '/';

        return {

        };
    });
}());